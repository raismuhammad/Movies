<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Movies" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Movies" parent="Base.Theme.Movies" >
        <item name="android:windowBackground">#001959</item>

        <!-- Secondary Color -->
        <item name="colorOnSecondary">@color/white</item>

        <item name="android:textColor">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Theme.ToolBar" parent="Widget.Material3.Toolbar">
        <item name="windowActionBar">false</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="Theme.AppBar" parent="Widget.Material3.AppBarLayout">
        <item name="colorPrimary">#22377E</item>
        <item name="colorSurface">#22377E</item>
        <item name="backgroundTint">#22377E</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="backgroundColor">#22377E</item>

        <item name="titleTextColor">@color/white</item>
        <item name="windowActionBar">false</item>
    </style>
</resources>